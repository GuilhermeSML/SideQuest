<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Side Quest</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto my-6">
        <!-- Activity Section -->
        <h2 class="text-2xl font-semibold text-center text-gray-700 mb-6">Do you accept the Side Quest?</h2>

        <% if (data) { %>
            <div class="activity mb-6">
                <!-- Display the Interest, Name, and Description -->
                <h3 class="text-xl font-semibold text-gray-800">
                    <%= data.Interests %>
                </h3>
                <h4 class="text-lg font-medium text-blue-600 mt-2">
                    <%= data.Name %>
                </h4>
                <p class="text-gray-600 mt-2">
                    <%= data.Description %>
                </p>
            </div>
            <% } else { %>
                <p class="text-gray-600">No activity available at the moment.</p>
                <% } %>


                    <!-- Accept Button Section -->
                    <form action="/api/acceptquest" method="POST" class="space-y-6">
                        <!-- Using EJS-rendered values -->
                        <input type="hidden" name="email" value="<%= email %>">
                        <input type="hidden" name="name" value="<%= data.Name %>">
                        <input type="hidden" name="description" value="<%= data.Description %>">
                        <div class="flex justify-between items-center space-x-4">
                            <button type="submit" name="action"
                                class="bg-blue-500 text-white py-2 px-4 rounded-md shadow hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Accept</button>
                        </div>
                    </form>
                    <!-- Back Button Section -->
                    <div class="mt-6 flex justify-center">
                        <a href="../"
                            class="bg-blue-500 text-white py-2 px-4 rounded-md shadow hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Back</a>
                    </div>
    </div>
</body>

</html>